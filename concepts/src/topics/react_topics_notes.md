  


#React Notes

1. props (properties)


any data type we can ABLE TO send as prop value    

useEffect - 
it is used to avoid the side effect of the react js

we can use 3 ways  useEffect hook

1- useEffect(callback) - it will execute the callback function for every re-render.
2- useEffect(callback, [])- it executes only one time ie in the initial load
3- useEffect(callback, [dependency])

note: useEffect hook will not return any data . (void function, void method)  